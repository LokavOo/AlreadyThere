# 迭代记录

## 1.9.1（2026-09-26）只修 exe 版
- 问题：v1.9 的 exe 在窗口里新建项目、运行时失败（"Failed to start embedded python interpreter"，找不到 encodings）。原因是单文件打包的 exe 每次运行先解压到临时文件夹；双击时先启动的那个进程打开窗口版后退出，删掉了临时文件夹，窗口版再启动自己时还沿用那个已删除的文件夹。用源码（bat）运行的不受影响。
- 修正：打包改为文件夹形式（`AlreadyThere.exe` 旁边带一个 `_internal` 文件夹），不再用临时文件夹；exe 启动自己时设置 `PYINSTALLER_RESET_ENVIRONMENT=1`。
- 自动测试补上漏掉的场景：用 exe 以窗口版在后台运行，再像用户一样发"新建演示项目"的请求，确认项目真的建出来，通过后才发布下载包。

## 1.9（2026-09-26）1.x 收尾版
依据诺贝尔奖题实跑（1.8，生产者全新、审查者全局，6 轮）复盘。查到 7 条，全部正确（6 条与存档的诺贝尔官网原文逐字吻合，1 条与维基原文吻合）；待复核 40 条、仅一方查到 8 条、未得出 4 条、资料缺载 1 条。查到比例低，主要是程序的两个问题：
- 独立来源检索：两个生产者都引用诺贝尔官网时，程序替审查者在全部可信网站里检索，前几条结果全是官网自己的页面（已被引用，不算独立），于是找不到独立来源（待复核 40 条里占 35 条）。改为只在生产者没引用过的可信网站里检索。
- 分几部分颁发的奖（如 2020 年物理学奖：一半授予彭罗斯，另一半授予根策尔和格兹，各有获奖原因）：1.8 只认一整句，模型把两句拼起来就判"未在原文中找到"（获奖原因核对失败 50 次的主因）。改为获奖原因可分段，用"；"隔开，每段都须在原文里逐字找到；两方比对时各段一一对应、顺序不论；一方照抄整句（含"另一半授予……"）、另一方分段的，各段都在那一整句里即算一致。名字比对另外忽略 Sir、Dame、Jr. 等称号。
- 一批里交对的条目先收下：不属于本批的、重复的块去掉，缺的条目记为本轮没交（窗口里提示），下一轮再补；不再整批重交（1.8 实跑格式重试 6 次）。
- 固定的数据文件夹：程序文件夹旁边的"数据"文件夹放设置、密钥、题目和全部项目，升级只要解压新版本。第一次启动时自动从程序文件夹及旁边的旧版本文件夹复制设置、密钥、题目、项目（只复制不删除，已有的不覆盖，记在"搬迁记录.txt"）。窗口直接列出 examples 里的题目（标"示例题目"），不用复制。
- 验证：诺贝尔奖实跑存档用 1.9 重做提交检查，第 1 轮通过的块从 94 增加到 104（共 120），原先获奖原因核对失败的 50 个块全部通过（都是分几部分颁发的奖）；三次明清存档重放与 1.8 全部一致。独立来源检索的修正要真实检索才能验证，尚未实跑。
- 单元测试 52 项。

## 1.8（2026-09-26）
第二道整理类题：近十年诺贝尔奖（2016—2025，六个奖，60 条）。口径经用户确认：经济学奖列入并注明正式名称；得主以原文（西文）名字为准；获奖原因以官网英文原句为准，另附模型的中文翻译（仅供参考，不核对）。
- 题目包可按项目选用：问题文件写 `"task": "nobel"` 用诺贝尔奖题包，不写的仍按事实汇编（出生日期）。新增 `tasks/nobel.py`（提示词、摘录规则、结论格式）和问题文件 `examples/近十年诺贝尔奖.json`。
- 新增核对器（`fields/record.py`）：人名名单——每个名字须出现在所引原文里，比对时忽略大小写、变音符号、中间名缩写和括号里的缩写，两方的名字集合相同才算一致；引文——获奖原因须是原文的逐字引用（忽略大小写、标点、开头的 for）。两者合成一个条目，两项都通过才算通过。
- 结论按奖项分表，统计由代码计算（每个奖的查到条数、查到条目的得主人数）；窗口消息按字段显示。
- 问题文件可覆盖个别流程参数：诺贝尔奖题每次调用处理 4 个条目（原为 6），避免输出过长被截断。
- 验证：新增模拟模型的端到端测试（带变音符号的名字、缺中间名缩写、机构得主、可信网站独立核验）；三次明清实跑存档用 1.8 重放，结论、条目块、审查、提示词与 1.7 全部一致。
- 单元测试 48 项。

## 1.7（2026-09-26）
重构，不改变结果；不需要实跑。
- 字段类型与程序核对器：新增 `guanzhe/fields/`。"出生日期"的核对（农历换算、公历写法、各方是否一致、给审查者的等价写法、从独立来源里提取日期、农历年干支）全部移进"日期"核对器 `DateField`，题目包里声明 `FIELD = DateField(key="出生日期", noun="出生")`，主流程只调用核对器，不再直接处理日期。以后换题（如诺贝尔奖）只需声明别的字段类型、写新的核对器。
- 修正一处老毛病：同一份存档，摘录给模型的原文片段会随 Python 的随机种子略有不同（同样长的人名、得分相同的片段排序不固定）。现在排序加了次序规则，每次结果一样，符合"记录可复现"的要求。
- 新增开发用的整体重放工具 `tools/重放.py`：用实跑项目存档里的模型答复和检索原文，把整个流程离线再跑一遍，逐条比对结论、逐块比对提交检查与审查结果、逐字比对发给模型的提示词。
- 验证：用三次实跑存档（1.5 全局、1.5 全新、1.6 全新）分别以 1.6（只加上面那处排序修正）和 1.7 重放，结论、每个条目块、每条审查、每次提示词全部一致。1.6 那次实跑的重放与原项目逐条、逐块一致。
- 还没动的：结论输出（report.py，生肖统计）和窗口里的消息显示（feed.py）仍按"出生日期"写，1.8 做诺贝尔奖题时一并改成按字段显示；各方一致的判断目前每个条目只有一个主字段，诺贝尔奖题（得主＋获奖原因）要扩成多个字段。
- 单元测试 46 项。

## 1.6（2026-09-26）
依据 1.5 的两次明清实跑（生产者全局 / 全新各一次，审查者都是全局）复盘。两次判为"查到"的条目都零错误（全局 18 条、全新 16 条）；全局跑满 6 轮，全新 2 轮。
- 农历换算：朝代提示只用来在同名年号之间挑，本朝没有这个年号时不再排除别的朝代。原因：朱元璋分在明朝组却生于元"天历"，努尔哈赤分在清朝组却生于明"嘉靖"，1.5 换算失败，两个生产者抄对了原文仍被判未查证、反复退回，全局那次第 3—6 轮只在处理这两条。
- 独立来源：审查者一次写几个编号（如"S00070,S00071"）时能识别；只认程序提供给审查者的独立来源，一个都对不上的"支持"改判为"无独立来源"。原因：1.5 认不出多个编号，却照样按"支持"判"查到"，独立验证次数记 0、网站名空白（这几条的来源核对过，确是维基、故宫、百度百科，结论本身没错）。
- 防止空转：某条没通过查证的各方，连续两轮交的关键内容（状态、出处、原句、出生日期、农历原文）一模一样、被拦原因也一样时，不再重复退回，按当前状态出结论，并注明"程序停止重复退回（停在第几轮，需人工查看）"。
- 改动比例（新颖度统计）的分母改为本轮实际重写的条目数。注：原型里这个统计只记录，本来就不用来提前停；真正的停止条件是各条都有终态且没有未闭合意见。
- 离线重放两次实跑：朱元璋、努尔哈赤两条，两方原先被拦的块都能换算并交审查（朱元璋 1328-10-21；努尔哈赤原文有"四月初八"与"正月二十八"两说，都在己未年）；有一处原文写"天历元年八月初八"，天历年号九月才启用，历表确实没有这一天，程序照旧不换算，这是对的。多编号独立来源共 9 处，核对都是程序提供过的可信网站来源。按旧数据，空转判定会让朱元璋停在第 3 轮、努尔哈赤停在第 5 轮。
- 单元测试 44 项。

## 1.x 后续计划（用户 2026-09-26 同意）
- 1.7：重构——把"出生日期"专用的逻辑从主流程挪出去，改成"字段类型＋程序核对器"（日期、人名名单、引文等），农历换算作为日期核对器的插件。
- 之后用第二道整理类题检验通用性，例如"近十年诺贝尔奖各奖项得主及获奖原因"（得主是人名名单，要处理译名；获奖原因要求逐字引用官网的官方表述）。

## 1.5（2026-09-26）
依据 1.4 实跑（误用了 16 人、没有可信网站名单的旧题）的复盘。
- 缺可信网站名单时醒目提示：新建项目选题时、选中项目时（窗口底部红条）、点运行时（要再确认一次）、运行日志和群聊里都会提示。删除旧题 `明朝皇帝生肖.json`（明清题已包含这 16 人）。
- 支持干支纪年：原文只写"己卯岁二月九日"的，生产者可以补上年号写成"建文元年己卯岁二月九日"（也可写"建文己卯岁二月九日"）。程序核对两点：原文里确实有"己卯岁二月九日"；补的年号那一年在历表中确实是己卯。两点都成立才换算，并标注"年号由生产者补出，干支与历表一致"。农历原文写了干支的，干支与历表对不上时不换算。
- 农历原文在所引原文里找不到时，从条目中剔除，标注"农历原文未在原文中找到（疑为模型记忆），已剔除"，不再交给审查者。判断"在原文里"时，允许原文在年号和月份之间夹一个括号注释（如"建文元年（1399年）二月初九日"），这种情况按找到处理，不剔除。
- 说明文字里的检索编号（S00123）、条目编号、块编号不再被当成数字。
- 窗口里选记忆模式：左栏"记忆模式"给每个角色一个下拉框（生产者：全局 / 固定 / 全新；审查者：全局 / 全新，审查者只区分是否全局）；选"固定"时可勾选看谁、填看几轮。保存时由程序写回 `设置.json`，只改各角色的 memory，其他字段原样保留，写之前先备份成 `设置.json.bak`。改了没保存就点运行，会先自动保存。
- 离线重放 1.4 实跑项目（不花钱）：朱厚照一条，DeepSeek 凭记忆写的农历（"……（甲戌月）二十四日（丁酉日）申时"）三轮都被剔除；朱瞻基一条，Kimi 第 3 轮补了年号的"建文元年己卯岁二月九日"由"未作换算"变为换算出 1399-03-16，其余几轮只写"己卯岁"的会提示可以补年号；DeepSeek 第 6 轮"建文元年二月初九日"（原文里夹了"（1399年）"）由"未找到"变为正确换算 1399-03-16。
- 单元测试 40 项。

## 2.0 待办（界面，已和用户商定）
- 运行记录改成群聊样式：头像加气泡，按角色区分（颜色、头像跟角色位置走，不跟厂商走）；头像素材由用户提供（可以是静态图，也可以是动图或短视频）。
- 群聊里"程序"发的消息，发言人显示为程序名"观者自在"，用用户提供的专门头像；原来的灰色小字改成带头像的消息。
- 左栏头像下方按角色标本项目 token 用量（输入、输出分开，失败的调用也算）；生产者另标检索次数。不标费用（估不准）。
- 后台检索记录（程序替谁检索、Tavily 等）默认隐藏：有一个专门的按钮，点了在单独的窗口里显示；主窗口只留一行，滚动显示后台记录的最新一行，后面跟时间戳，用来看程序在跑还是卡住。
- 生产结果卡片（原句、出处、✓/⚠ 核对状态）保持现样式，放进该角色的气泡里。
- 消息带附件（md 文件）时，气泡下方显示文件卡片，可预览、下载。
- 头像：左栏角色处的头像显示四种状态——空闲（微动）；工作（调用已发出、正常等待，动效稍强并换动作）；卡了（API 层面：调用发出后超过一定时间仍无返回，先定 60 秒，按实跑调整）；挂了（欠费、密钥错误等需要用户处理的问题）。每个角色四段素材，由用户提供。气泡旁用固定头像。需要补一个"开始调用"的信号并记下发出时间。
- 保留实时逐条追加、底部按钮和左侧项目栏。主题以后做成可切换的皮肤，这次不管。

## 1.4（2026-09-25）
依据明清 28 帝第一次真实运行（1.2，4 轮）的复盘。
- 摘录：按本名、年号、庙号找人（去掉括号），优先挑讲到"生/诞"的段落；按相关程度挑来源，不再只给最新 4 条；同一原文只列一次。
- 原句比对：先去掉网页排版符号（链接、表格竖线、脚注），再忽略空白、标点、繁简差异；模型看到的摘录就是这份去排版文本。
- 农历：生产者照抄农历原文，程序按历表换算公历（1582-10-15 以前儒略历），并与所填公历核对；原句里有年号农历写法而没填的，程序也会识别核对。"年号纪年＋公历月日"的混写按写法区分，判不准的交审查者。
- 生肖按农历年干支确定，取消"1 月 1 日至 3 月 2 日不计"的排除。
- 记忆模式：全局 / 固定 / 全新，每个角色可设；非全局模式下，意见中不含他方的数值与出处，检索词一步同样处理；生产者只能引用本方检索。
- 审查者分两项判断：原句与所填日期是否等价、可信网站上的独立来源是否支持；两项分开记录。
- 规则（经用户确认）："查到"须两方一致＋原句等价＋另一可信网站（生产者引用的网站以外）独立支持，两方引用同一网站也可以；可信网站之间说法不同时判"有争议"，不按多数定；写法不同（儒略/格里外推、农历/公历、年份/完整日期）由程序换算，换算后同一天不算矛盾；日期有争议但都在同一农历年的，生肖计入统计并标明。新增"待复核""仅一方查到"；同年份的"只有年份"与"完整日期"视为一致；已定论的条目后续轮次不再重写；最多 6 轮。
- 修正：Windows 上建的项目在其他系统上读不出存档（路径反斜杠）。
- 用第一次运行的存档离线重放（不花钱）：清朝条目的摘录里有正确生年的从 4/24 升到 24/24（明朝 29/32 → 32/32）；当时被误拦的 41 处原句全部能对上；3 处把"洪武三十一年"换算错的、1 处换算差一天的被程序拦下。
- 单元测试 34 项。

## 1.3（2026-09-25）
- 新增窗口版：双击 `启动观者自在.bat` 打开应用窗口（Edge 应用模式），以群聊形式显示过程；可在窗口里做连通测试、新建项目、运行、看结论、打开详细记录。
- 窗口版只监听本机（127.0.0.1）；密钥只显示已填/未填；运行在独立进程中，输出写入项目文件夹的 `运行日志.txt`，关窗口不打断运行。
- 新增 `python -m guanzhe app` 命令；新增 1 项单元测试（群聊消息翻译），共 22 项。

## 1.2（2026-09-25）
- 正式启用项目名"观者自在 (AlreadyThere)"：命令改为 `python -m guanzhe ...`（原为 `python -m mmv ...`），页面与说明中的名称同步更改。
- 问题文件的条目可带 `group`（分组）；结论按分组分别列出，生肖统计为全部合计。
- 新增问题文件 `明清皇帝生肖.json`：明朝 16 位、清朝 12 位（含努尔哈赤、皇太极），共 28 人，口径经用户确认。
- 使用说明改为以 Anaconda Prompt 为主的步骤。
- 新增 1 项单元测试（分组输出），共 21 项。

## 1.1（2026-09-25）
- 设置里每个角色可加 `extra`（原样附加到请求里的参数，如关闭深度思考）与 `max_tokens`（输出上限）；两项的实际取值随每次调用记入账本。
- 默认角色：生产者 1 DeepSeek（deepseek-v4-pro）、生产者 2 Kimi（kimi-k2.6）、审查者 豆包（doubao-seed-2-1-pro-260915），三家均关闭深度思考。
- 新增 1 项单元测试，共 20 项。
- 首次用真实接口完成连通测试（四项全部连通）。

## 1.0（2026-09-24）
- 原型第一期：2 个生产者 + 1 个审查者；程序检索并存档原文，机械核对原句与数字；四种结论；哈希链账本；浏览器过程查看；演示可跑。
