# 观者自在 (AlreadyThere) · 原型第一期 使用说明

## 这一期能做什么

- 2 个生产者（不同模型）各自联网检索、各自写出每个条目的结果和出处；1 个审查者逐条核对。
- 程序机械检查：出处是否真的存在、摘抄的原句是否真的出现在存档原文里、数字是否对得上。编造的原句会被当场标出，并要求重做。
- 每个条目最终是以下结论之一（1.4 起）：
  - **查到**：两个生产者各自查到且一致、审查者判定原句与所填日期等价，并且审查者在**另一个可信网站**（生产者引用的网站以外）找到原文独立支持。两方引用同一网站也可以，只要另一可信网站独立支持；
  - **待复核**：两方一致，但没有另一可信网站的独立来源支持；
  - **仅一方查到**：只有一方通过查证；
  - **有争议**：两方不一致，或者独立来源和两方的结果矛盾。不按多数定，因为网站之间常互相转抄；
  - **资料缺载** / **未得出**。
  "查到"的意思是"从公开的可靠网站能查到、并能相互印证"，用来防网络谣言，不等于史学考证的定论。
- 写法不同不算矛盾：儒略历和按格里历外推的写法、年号农历和公历、只写年份和完整日期，程序换算后是同一天（同一年）的视为一致；审查者看到的也是程序换算好的各种等价写法。
- 生肖统计计入"查到"，以及"有争议"但各种说法都落在同一个农历年的条目（生肖不受影响，结论中会标明）。
- 原文是年号农历的（如"建文元年二月初九日"），生产者只需照抄农历原文，公历由程序按历表换算（1582 年 10 月 15 日以前用儒略历）；生肖按出生当天所在农历年的干支确定。归类和统计全部由代码计算，不由模型计算。
- 全过程写进一本不能悄悄修改的账本（哈希链），每次模型调用的完整输入和原始返回都存档，可在浏览器里逐条查看。

这一期还没有：准入门（由你手动放行）、找茬者、记录者、解说者、论文和专家审阅页。这些在第二、三期加。

## 第一次使用：先跑演示（不需要任何密钥，不花钱）

1. 需要 Python 3.9 或更高版本。装过 Anaconda 的，它已自带 Python，不用另装；没有的到 python.org 下载，Windows 安装时务必勾选"Add Python to PATH"。
2. 把本文件夹解压到任意位置。下文说的"程序文件夹"，指 `使用说明.md` 和 `guanzhe` 文件夹所在的那一层。
3. 打开终端并进入程序文件夹：
   - Windows（装了 Anaconda）：打开开始菜单里的 Anaconda Prompt，输入 `cd /d `（d 后面有空格），把程序文件夹拖进窗口，按回车。
   - Windows（没装 Anaconda）：在程序文件夹空白处按住 Shift 点右键，选"在终端中打开"。
   - Mac：打开"终端"，输入 `cd `（cd 后面有空格），把程序文件夹拖进终端窗口，按回车。
4. 输入下面这行并回车：

   ```
   python -m guanzhe demo 演示项目
   ```

   Mac 上如果提示找不到 python，把 `python` 换成 `python3`（下同）。

5. 屏幕上会打印结论表。再输入：

   ```
   python -m guanzhe serve 演示项目
   ```

   浏览器会自动打开过程查看页面。看完回到终端按 Ctrl+C 结束。

演示里的人物和网页全部是虚构的测试数据。演示故意安排了三种情况：一个生产者编造了一句原文里没有的原句（程序会拦下并要求重做）、两个来源说法不一（会登记为有争议）、查不到资料（会如实报资料缺载）。

## 窗口版（1.3 版新增，推荐）

双击程序文件夹里的 **启动观者自在.bat**，会打开一个"观者自在"窗口（用 Windows 自带的 Edge 以应用模式打开，没有地址栏）。在窗口里可以：

- 看准备情况：每个角色用哪个模型、密钥填了没有（只显示已填/未填，不显示密钥内容）；
- 点「连通测试」；
- 点「＋ 新建项目」，选题目（也可以选"演示"）；
- 点「开始运行」，像群聊一样看几个模型怎么查、怎么被程序核对、怎么被审查；
- 点「看结论」「详细记录」。

运行在后台进行：关掉窗口不会打断，重新双击打开能接着看；每次运行的输出存在项目文件夹里的 `运行日志.txt`。
双击没反应或提示找不到 Python 时，也可以在 Anaconda Prompt 里进入程序文件夹，输入 `python -m guanzhe app`。

## 数据文件夹（1.9 版起）

程序文件夹旁边会自动出现一个"数据"文件夹（例如程序在 `E:\观者自在\原型\观者自在(AlreadyThere)_v1.9`，数据就在 `E:\观者自在\原型\数据`）。设置、密钥、你自己的题目和全部项目都放在这里。

- 升级：把新版本解压到旧版本旁边，双击新版本的 `启动观者自在.bat`。第一次启动时，程序会把旧版本文件夹里的设置、密钥、题目、项目复制到数据文件夹（只复制，不删除原来的；复制了什么记在数据文件夹的"搬迁记录.txt"，窗口左栏也会显示）。确认没问题后，旧版本就可以移进"历史版本"。
- 以后改设置、填密钥，都改数据文件夹里的那份。
- examples 里的题目，新建项目时直接能选（标"示例题目"），不用再复制。

## 用真实模型跑：要准备的三个文件

都在 `examples` 文件夹里，复制一份到程序文件夹下再改：

1. **密钥**：把 `密钥.env.模板` 复制一份，改名为 `密钥.env`，在等号后面填入你的密钥。这个文件只留在你电脑上，不要发给任何人。
2. **设置**：`设置.json`。把每个角色的 `model` 一栏改成你账户里能用的模型名（在各厂商控制台的模型列表里能查到）。默认安排是：生产者 1 用 DeepSeek，生产者 2 用 Kimi，审查者用豆包。
3. **问题**：`明清皇帝生肖.json`（明清两朝 28 位皇帝，口径已确认，带可信网站名单）。旧的 `明朝皇帝生肖.json` 在 1.5 版已删除；如果你的程序文件夹里还有它，请删掉或移走。问题文件里每个条目可以带 `group`（分组，例如朝代），结论会按分组分别列出，生肖统计为全部合计。

用记事本修改 JSON 文件时注意：引号、逗号、冒号都要用英文半角；保存时选 UTF-8 编码。

## 用真实模型跑：步骤

```
python -m guanzhe ping --settings 设置.json --secrets 密钥.env
```
逐个测试各模型和检索能不能连通（只花极少费用）。全部显示"连通"再往下走。

```
python -m guanzhe new 明清项目 --project 明清皇帝生肖.json
python -m guanzhe run 明清项目 --settings 设置.json --secrets 密钥.env
```
运行结束会打印结论表；结果文件在 `明清项目/outputs/结论.md`。

```
python -m guanzhe serve 明清项目
```
在浏览器里查看全过程：每个模型实际看到了什么、原样返回了什么、每条检索存档的原文。

```
python -m guanzhe verify 明清项目
```
校验账本和存档文件有没有被改动过。


## 设置里可选的两项（1.1 版新增）

- `extra`：原样附加到请求里的参数。例如 `"extra": {"thinking": {"type": "disabled"}}` 表示关闭深度思考。第一期默认三家都关。
- `max_tokens`：该角色的输出上限。开启深度思考时，有的厂商要求至少 16000，否则答案会被截断。
- 这两项的实际取值会随每次调用一起记进账本。

## 如果中途停了

程序遇到调用失败会先自动重试（先短间隔 3 次，再长间隔 3 次），仍失败就暂停并说明原因，例如密钥不对、余额不足、网络不通。处理好以后，重新运行同一条 `run` 命令即可从停下的地方继续：已经成功的调用和检索不会重复发出、不会重复花钱。

## 网络提示

- 访问国外接口（Anthropic、Tavily）如果连不上，可能需要设置代理。程序会自动使用系统环境变量 `HTTPS_PROXY` 里的代理。
- Mac 上如果报"SSL 证书校验失败"，到"应用程序 / Python 3.x"文件夹里双击运行 `Install Certificates.command`，再重试。

## 记忆模式（1.4 版新增；1.5 起在窗口里选）

模型每次被调用时都是"失忆"的，它能记得什么，完全取决于程序这次给它看什么。

**在窗口里选（推荐，不用改文件）**：窗口左栏最下面是"记忆模式（运行前选好）"，每个角色一个下拉框。
- 生产者可以选 全局 / 固定 / 全新；审查者可以选 全局 / 全新（对审查者来说只区分是不是"全局"）。
- 选"固定"时下面会多出两项：勾选看谁的结果（自己、另一个生产者），填看最近几轮（1 到 6）。
- 选好后点"保存记忆模式"。程序会把选择写回 `设置.json`，只改记忆模式这一项，其他内容不动；改之前的文件自动备份成 `设置.json.bak`（想还原就把它改名回 `设置.json`）。
- 改了没保存就点"开始运行"，程序会先自动保存再运行。
- 正在运行的项目不受影响，新设置从下一次运行开始生效。

几种模式的意思（`设置.json` 里的写法，一般不用手改）：

| 写法 | 意思 |
|---|---|
| `{"mode": "全局"}` | 能看到所有生产者之前的结果，以及意见原文 |
| `{"mode": "固定", "see": ["self"], "rounds": 1}` | 只看 `see` 里列的角色（`self` 表示自己），最近 `rounds` 轮 |
| `{"mode": "全新"}` | 每轮从头开始：只看问题、口径、本轮检索，以及针对自己的意见 |

默认：生产者"固定"（只看自己上一轮），审查者"全局"。
不论哪种模式，问题和口径都照给；除"全局"以外，程序发给生产者的意见里会把对方的数值和出处去掉（例如只说"另一位生产者查到的日期与你的不一致"），检索词那一步也按同样的规则。生产者只能引用自己检索到的原文。

想比较两种模式时，建两个项目分别跑：在窗口里先把两个生产者都选成"全局"、保存、跑一个项目；再都选成"全新"、保存、新建另一个项目跑。

## 可信网站（1.4 版新增，只给审查者用）

问题文件里的 `trusted_sites` 列出审查者做独立核验时用的网站，明清这道题是：中文维基百科、故宫博物院、百度百科、中国哲学书电子化计划。
审查者先在已存档的网页里找这些网站上、生产者引用的网站以外的原文；找不到时，程序在这几个网站内检索一次，检索词由程序生成（人名＋出生），不带任何日期数字。
生产者的检索不受这个名单限制。

题目文件里没有 `trusted_sites` 时，独立核验不会启动，任何条目都判不成"查到"。1.5 起，新建项目选这种题目、选中这种项目、点运行时，窗口都会用红字提醒，运行日志里也会写一条警告。

## 诺贝尔奖题（1.8 版新增）

- 题目文件：`近十年诺贝尔奖.json`（2016—2025 年，物理、化学、生理学或医学、文学、和平、经济学六个奖，共 60 条）。和别的题目文件一样，复制到程序文件夹，在窗口里新建项目时选它。
- 每条要查两项：得主（照原文写原名，多位用"；"隔开）和获奖原因（官网英文原句，逐字引用）。程序核对名字和原句都真在原文里；另附的中文参考是模型翻译，没有核对。
- 条目比明清题多一倍，花费和检索次数也大约多一倍；建议两个生产者都选"全新"。

## 已知限制（第一期）

- 生肖按农历年的干支确定（1.4 起），依赖历表数据；历表里查不到的日期不换算。
- 原文只写干支纪年（如"己卯岁"）、没写年号的，要生产者补上年号，程序才能换算（1.5 起）。
- 检索接口返回的全文是接口自己从网页提取的正文，程序标为"接口提取正文"，不是网页原始文件。
- "来源互相独立"目前按网站域名和正文哈希判断：同一网站或同一正文不计独立。镜像站点暂时识别不了。
- 账本哈希链能发现中间被改动；要发现末尾被整段删除，请把 `verify` 打印的最后一条哈希另外记下，下次对照。
- 费用上限、找茬者、准入门等尚未实现。


## 历表数据的来源与许可

农历换算用的是法鼓文理学院（DDBC/DILA）时间规范资料库的年号月表（2012-02 版），许可为 CC BY-SA 3.0。说明与许可全文在 `guanzhe/data` 文件夹里。
